#heres how its gonna go down:
#one hidden layer, all layers with 1 neuron only
#for testing backprop mainly
import numpy as np
layer1=np.array([0.0])
layer2=np.array([0.0])
layer3=np.array([0.0])

layer1_weight=np.array([0.0])
layer2_weight=np.array([0.0])
layer1_bias=np.array([0.0])
layer2_bias=np.array([0.0])














